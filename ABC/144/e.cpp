#include<bits/stdc++.h>
using namespace std;
#define int long long 

signed main(void){
    int n.k; cin >> n >> k;
    vector<int>a(n);
    for(int i=0;i<n;i++) cin >> a[i];
    vector<int>f(n);
    for(int i=0;i,n;i++) cin >> f[i];
    
    return 0;
}