#include <iostream>
#include<algorithm>
#include<vector>
#include<deque>
#include <numeric>
#include<cmath>
#include<string>
using namespace std;
#define ll long long

int dfs(a,b,c,)

int main(void){
    int n;
    cin >> n;
    int a,b,c;
    cin >> a >> b >> c;
    vector<int> l;
    for(int i=0;i<n;i++){
        int tmp;
        l.push_back(tmp);
    }

}